# Euclid — Rubric / SOP Author Distribution
name: euclid
version: 1.1.0
description: "Euclid — Rubric / SOP Author for the Scaled CS platform. Defines audit checklists, scoring rubrics, and SOPs. Definitions only; zero data access, ever."
hermes_requires: ">=0.13.0"
author: "Angel Ancheta"
license: "MIT"

env_requires:
  - name: OPENROUTER_API_KEY
    description: "OpenRouter API key for model access"
    required: true
